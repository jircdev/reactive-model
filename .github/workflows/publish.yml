name: Publish to NPM

on:
    push:
        branches:
            - deploy/npm

jobs:
    build-and-publish:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '18'
                  registry-url: 'https://registry.npmjs.org'

            - name: Install dependencies
              working-directory: src
              run: |
                if [ -f package-lock.json ]; then
                  npm ci
                else
                  npm install
                fi

            - name: Build package
              working-directory: src
              run: npm run build

            # IMPORTANT: NPM_TOKEN must be an Automation Token (not a regular token)
            # Automation Tokens don't require OTP and are designed for CI/CD
            # Create one at: https://www.npmjs.com/settings/[username]/tokens
            # Select "Automation" token type, then add it to GitHub Secrets as NPM_TOKEN
            - name: Publish to NPM
              working-directory: src
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
              run: npm publish --access public
